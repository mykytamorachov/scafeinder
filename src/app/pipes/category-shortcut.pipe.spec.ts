import { CategoryShortcutPipe } from './category-shortcut.pipe';

describe('CategoryShortcutPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryShortcutPipe();
    expect(pipe).toBeTruthy();
  });
});
